# M1 Windows 端到端报告

> 对应：技术设计 §11 M1。日期：2026-09-19。代码：本 commit；演示脚本：`demos/m1-demo.ps1`。
> 纪律声明（显式失败）：本报告区分"已验证 / 部分验证 / 未验证"，不以演示未完成掩盖缺口。

## 已验证（管理员实机运行，ETW 实时数据）

| 能力 | 证据 |
|---|---|
| ETW 事件源（经典 kernel logger：PROCESS + IMAGE_LOAD + FILE_IO + FILE_IO_INIT + TCP_IP；Dns-Client by GUID 独立 UserTrace） | 服务日志：kernel 事件 200 万+/采样周期，DNS 事件正常流入（QueryName ~92%） |
| 进程身份表与继承链 | `[exec]` 日志：fake_harness 命中特征库自成根 → 子进程（powershell/tar/conhost）root 正确继承 |
| exe 全路径（ImageLoad FileName + QueryDosDevice 转盘符） | `[exec] exe=D:/develop/.../fake_harness.exe` |
| 进程 cwd 读取（PEB RTL_USER_PROCESS_PARAMETERS.CurrentDirectory @0x38） | `[exec] cwd=D:\...\target\demo` |
| CommandLine 读取（PEB @0x70；x64；曾误用 0x80 指向 Environment，已修） | 修复后待下一轮演示确认（见未验证） |
| 持久化检测（RunKey/RunOnce 轮询快照对比） | 演示场景 D：`persistence Audit` 判定落库 + UI 可查 |
| 规则快照编译/热替换（arc-swap）、白名单/配置 API 重建快照 | `/api/whitelist`、`/api/config PUT` 编译通过，逻辑同源 |
| SQLite 存储层（WAL、schema、批量写 200ms/500 条、追加触发器、每日清理为唯一 DELETE 路径） | verdicts/events/dns_map/processes 落库，`/api/verdicts` 可查 |
| Web UI（127.0.0.1:8377 + 随机 token Bearer + Host 校验 + rust-embed 静态页） | `/api/status`、`/api/verdicts` 实测返回；Dashboard/事件流/白名单/设置四页 |
| 早过滤（非监控树进程即读即弃） | kernel 2.3M 事件 → 仅 ~700 进入引擎 |
| 启动补扫描（Toolhelp32 + QueryFullProcessImageNameW + ppid 迭代继承） | 启动日志"补扫描 N 个监控树进程" |
| Name 迟到重试（unknown FileObject 挂起，Name 到达补发） | 实现 + unknown 计数（待演示完整验证，见下） |

## 部分验证 / 未验证（显式失败）

1. **场景 A（.git 读取阻断）未在演示中触发**。根因（日志实证）：经典 ETW FileIo 的
   FileName 大量为**进程相对名**（`config`、`repo`、组件探测名），需按进程 cwd 拼接
   （已实现）；但 burst 场景下 Name 事件缺失率高（监控树进程 84 resolved / 4010 unknown），
   pending 重试也未能覆盖全部。诊断日志已加（`[file-unknown]`）。
   **缓解路径（M4）**：FileObject 缓存换 LRU + 对 unknown 的 Create 事件用
   NtQueryInformationFile 自查（同 pid 补一个句柄查询）或接受 unknown 披露。
2. **场景 B（命令封堵）依赖 CommandLine 读取**：偏移已修（0x70），编译通过，
   因 UAC 演示中断未复跑确认。继承链与 argv 匹配逻辑单测覆盖（`命令封堵_导出型命令命中`）。
3. **场景 C（网络阈值阻断）端到端未确认**：TCP-IP 事件字段解析在 M0 全通过，
   ConnRegistry/阈值/处置链单测通过；实机 loopback 连接的 conn_id 关联
   （connect 与 send 事件的四元组一致性）待演示确认，已加 `[net]` 诊断日志。
   另注意回环封 IP 已显式跳过（防自断本机服务）。
4. **OS 通知（Toast）**：WTS + CreateProcessAsUser + PowerShell AUMID Toast 链路已实现
   （控制台模式降级本会话直弹），演示被中断，通知弹窗未人工确认。
5. 演示脚本第 15 轮触发 **Windows Defender ML 误报**（Trojan:Win32/Commando.A!ml）：
   旧版场景 C 用 `-EncodedCommand + TcpClient 裸写随机数据` 模拟外传，特征确似木马。
   已改为常规工具链行为（文本载荷→fsutil 全零文件、curl 标准 POST、去 EncodedCommand）。
   教训记录：**模拟攻击行为的演示本身要按"正常工具用法"构造，否则演示环境先杀演示**。

## 与设计的偏差（已显式登记）

| 偏差 | 设计原文 | M1 现状 | 归位 |
|---|---|---|---|
| 封 IP 用 netsh advfirewall | 用户态 WFP FwpmFilterAdd0 | netsh 临时规则 + TTL 删除（同一防火墙栈） | M4 |
| 持久化=注册表轮询 | TaskScheduler ETW + RunKey，归因发起进程 | 仅 RunKey/RunOnce 轮询，pid 归因缺失（pid=0） | M4 |
| /api/stream SSE 未实现 | query token 仅放行 SSE | UI 3s 轮询 verdicts/events 替代；SSE 返回 501 说明 | M4 |
| 服务宿主 | windows-service + 恢复策略 | 控制台管理员模式 | M4 |
| 停机序列 §9.3 | 未决 PERM 回 FAN_ALLOW 等 | Linux 专属；Windows 侧已做"启动清理陈旧 ETW 会话"（强杀残留会话导致新实例 0 事件的实测教训） | M4 |
| 断连接 IPv6 | SetTcp6Entry | windows crate 0.61 未导出，显式报错 | M4 直调 iphlpapi |

## 复跑演示

管理员 PowerShell：`powershell -ExecutionPolicy Bypass -File demos\m1-demo.ps1`
（产物 `target/demo/`，服务日志 `svc.out.log` 含 `[exec]/[file]/[file-unknown]/[net]` 诊断行，
结果打印到控制台；预期四类判定：git-dir Block / cmd-block Block / net-threshold Block / persistence Audit）。
