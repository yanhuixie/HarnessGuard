# M1 Windows 端到端报告

> 对应：技术设计 §11 M1。日期：2026-09-19。代码：本仓库；演示脚本：`demos/m1-demo.ps1`。
> 纪律声明（显式失败）：本报告区分"已验证 / 部分验证 / 未验证"，不以演示未完成掩盖缺口。

## 最终演示结果（2026-09-19 23:04，管理员实机，demos/m1-demo.ps1）

| 场景 | 结果 |
|---|---|
| B 命令封堵 | ✅ **端到端打通**：ETW Exec → PEB CommandLine → 身份继承 → 规则命中 → `cmd-block Block` 判定落库 + 杀进程处置 + UI 可查（证据含完整命令行） |
| D 持久化告警 | ✅ RunKey 轮询检测 → `persistence Audit` 判定 + UI 可查 |
| C 网络阈值 | ⚠️ **归因链验证到字节累计入口**：`[conn-tx]` 日志实证（pid=curl、root=fake-harness、目标外部端点正确）；**阈值跨越未实测**——本机 TCP-IP send 事件呈内核采样态（45s 全系统仅 106 条；8MB body 仅产生 1 条 176B 头部事件），累计字节无法达到阈值。M4 评估字节计数替代路径（GetPerTcpConnectionEstats 轮询 / WFP callout） |
| A .git 读取 | ❌ 未触发（详见下节）：判定管线本身已由 B 场景证明（同为 ETW 事件→规则→判定→处置链），卡在 ETW 文件名解析 |

## 已验证（管理员实机运行，ETW 实时数据）

| 能力 | 证据 |
|---|---|
| ETW 事件源（经典 kernel logger：PROCESS + IMAGE_LOAD + FILE_IO + FILE_IO_INIT + TCP_IP；Dns-Client by GUID 独立 UserTrace） | 服务日志：kernel 事件 100 万+/采样周期，DNS 事件正常流入（QueryName ~92%） |
| 进程身份表与继承链 | `[exec]` 日志：fake_harness 命中特征库自成根 → 子进程（powershell/tar/curl/conhost）root 正确继承 |
| exe 全路径（ImageLoad FileName + QueryDosDevice 转盘符） | `[exec]` 日志完整路径 |
| 进程 cwd 读取（PEB CurrentDirectory @0x38） | `[exec] cwd=D:\...\target\demo` |
| CommandLine 读取（PEB @0x70；曾误用 0x80 指向 Environment，已修） | B 场景判定证据含完整命令行 |
| 短命进程 cmdline 竞态兜底（空时按 exe 名匹配封堵模式） | B 场景（evilpack 偶发 cmdline 空，仍命中 `evil*`） |
| 网络归因（事件 PID=-1 → GetExtendedTcpTable owner 表；connect 缺失 → send 补登记） | `[conn-tx]` 日志 |
| 持久化检测（RunKey/RunOnce 轮询快照对比） | D 场景判定落库 + UI 可查 |
| 规则快照编译/热替换（arc-swap）、白名单/配置 API 重建快照 | `/api/whitelist`、`/api/config PUT` |
| SQLite 存储层（WAL、批量写、追加触发器、每日清理唯一 DELETE 路径） | verdicts/events/dns_map/processes 落库 |
| Web UI（127.0.0.1 + 随机 token Bearer + Host 校验 + rust-embed） | `/api/status`、`/api/verdicts` 实测返回 |
| 早过滤（非监控树即读即弃） | kernel 100 万+ 事件 → 引擎约 700-10000 事件 |
| 启动补扫描（Toolhelp32 + QueryFullProcessImageNameW + ppid 迭代） | 启动日志 |
| 启动清理陈旧 ETW 会话（强杀残留会话导致新实例 0 事件——实测教训） | 二次启动正常出流 |

## 未验证 / 已知缺口（显式失败）

1. **场景 A（.git 读取阻断）**。多轮日志实证：经典 ETW FileIo 的 FileName 大量为
   **进程相对名**（`config`、`repo`、组件探测名），cwd 拼接已实现；但 cmd/certutil/tar
   的 burst 打开 Name 事件缺失率 ~99%（4400+ unknown），pending 重试等不到 Name 事件。
   缓解路径（M4）：FileObject 缓存换 LRU + 对 unknown 的 Create 事件补一次同路径自查
   打开（拿句柄后 NtQueryInformationFile）；或评估 Security 4663 审计通道（需开 SACL）。
   规则正确性由单测覆盖（`快路径_harness_触碰_git_阻断` 等四例）。
2. **场景 C 阈值跨越**：见上表内核采样限制；ConnRegistry/阈值判定有单测覆盖，
   **engine→enforcer 处置执行链无单测**（该空档曾掩盖 drop_tcp 端口字节序 bug，
   已由独立评审发现并于评审修复 commit 修正）。
3. **杀进程对已退出进程**报 0x80070057 属预期（判定与处置仍正确落库）。
4. OS 通知（Toast）：WTS + CreateProcessAsUser + PowerShell AUMID 链路已实现
   （控制台模式降级本会话直弹），弹窗未人工确认。

## 与设计的偏差（已显式登记）

| 偏差 | 设计原文 | M1 现状 | 归位 |
|---|---|---|---|
| 封 IP 用 netsh advfirewall | 用户态 WFP FwpmFilterAdd0 | netsh 临时规则 + TTL 删除（同一防火墙栈） | M4 |
| 持久化=注册表轮询 | TaskScheduler ETW + RunKey，归因发起进程 | 仅 RunKey/RunOnce 轮询，pid 归因缺失 | M4 |
| /api/stream SSE 未实现 | query token 仅放行 SSE | UI 3s 轮询替代；SSE 返回 501 说明 | M4 |
| 服务宿主 | windows-service + 恢复策略 | 控制台管理员模式 | M4 |
| 断连接 IPv6 | SetTcp6Entry | windows crate 0.61 未导出，显式报错 | M4 直调 iphlpapi |

## 实测工程教训（入报告备查）

- UAC 弹窗无人值守 ~2 分钟自动超时按取消处理。
- PowerShell 双引号串里 `$lan:18080` 的 `lan:` 被解析为驱动器限定符——须 `${lan}:18080`。
- 模拟外传的演示（EncodedCommand + TcpClient 裸写随机数据）触发 Defender ML 误报
  （Trojan:Win32/Commando.A!ml）——模拟攻击的演示要按正常工具用法构造。
- 本机网络事实：回环与自连（LAN IP→自身）大流量均不产生 TCP-IP send 事件；多网卡环境
  取默认路由出口 IP 用 UDP-connect 法。

## 复跑演示

管理员 PowerShell：`powershell -ExecutionPolicy Bypass -File demos\m1-demo.ps1`
（产物 `target/demo/`，服务日志 `svc.out.log` 含 `[exec]/[file]/[file-unknown]/[net]/[conn-tx]`
诊断行；场景 C 依赖外网 httpbin.org 可达）。
