[package]
name = "hg-plat-macos"
description = "HarnessGuard macOS 适配：auditpipe BSM 解析、libpcap 网络、tcpdrop/pf 处置（技术设计 §5.3）"
version.workspace = true
edition.workspace = true
publish.workspace = true

# macOS 专属依赖（libpcap-sys 等）在代码落地时以 workspace 级
# [target.'cfg(target_os = "macos")'.dependencies] 组织，确保 Windows 上
# cargo check --workspace 全绿（技术任务硬约束 3、4）。
[dependencies]
