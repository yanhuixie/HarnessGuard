[package]
name = "hg-plat-win"
description = "HarnessGuard Windows 适配：ETW 事件源、杀进程/断连接/WFP 封禁处置（技术设计 §5.1）"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]

[dev-dependencies]
# M0 spike（examples/etw_spike.rs）：ETW 消费 + RSS 自测
ferrisetw = "1"
windows-sys = { version = "0.60", features = ["Win32_Foundation", "Win32_System_Threading", "Win32_System_ProcessStatus"] }
