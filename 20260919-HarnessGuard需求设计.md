# HarnessGuard 需求设计

> 本文档基于 `intro.md` 经逐项评审（grilling）达成的共识整理而成，是后续开发的基准文档。
> 日期：2026-09-19

---

## 1. 项目定位

HarnessGuard 是一个跨平台（Windows / Linux / macOS）的本机安全监控工具，监控 Claude Code、Codex、Cursor 等 AI Harness 软件及其子进程的行为，**侦测并自动阻断**针对用户源代码等资产的打包外传行为。

### 1.1 威胁模型

**防"被动外传"**：harness 因误操作、被提示词注入、或其依赖被供应链投毒，在非用户明确意图下将源代码 / 仓库内容 / 凭据外传。harness 本身不主动对抗 HarnessGuard。

**不防"定向恶意逃逸"**：harness 拥有与用户同等的 shell 权限，可主动杀死监控进程、篡改配置。用户态安全工具对此没有必胜路径（除非内核驱动 + 签名，已明确排除）。

### 1.2 能力边界（须在产品文档中明示）

1. **可被绕过**：用户态工具可被同等权限的恶意进程停用或篡改（威胁模型限定的范围外）。
2. **macOS 无实时文件阻断**：仅能做到检测全可见 + 秒级事后处置（掐连接 / 杀进程），已发出的数据拦不住。
3. **TLS 无内容检测**：HTTPS 流量看不到明文，判定依赖元数据启发式（详见 3.2）。
4. **DoH/DoT 退化**：加密 DNS 下域名→IP 关联失效，端点判定退化为按 IP，白名单维护变难。

---

## 2. 核心决策汇总

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 响应模式 | 自动阻断 + 白名单豁免 + OS 级通知（非浏览器弹窗） |
| 2 | 网络层定位 | 兜底防线：上行体量阈值（默认 100MB，可预设可自设）+ 端点白名单 |
| 3 | 文件层规则 | 敏感文件读取→审计；读 `.git`→默认阻断；打包行为→默认阻断（均允许用户改配置） |
| 4 | 豁免机制 | 身份矩阵 + 导出型命令封堵（`git archive`/`bundle`/`format-patch`） |
| 5 | 平台能力阶梯 | Linux/Windows 实时阻断；macOS 检测全可见 + 事后秒级处置；统一"事件流 + 处置动作"架构 |
| 6 | 实现语言 | Rust（决定性因素：三平台 native API 绑定成熟 + 100MB 内存预算） |
| 7 | 威胁模型 | 防"被动外传" + 基础自加固，不承诺不可绕过 |
| 8 | 部署架构 | 单特权系统服务 + 内嵌 Web UI（127.0.0.1 + 随机 token + Host 校验），安装时一次提权 |
| 9 | 日志存储 | SQLite 追加写 + ACL 防篡改，默认保留 30 天 / 500MB（先到为准，均可配） |

---

## 3. 检测与响应规则

### 3.1 文件系统层（第一道防线）

| 规则 | 默认动作 | 可配置 | 说明 |
|------|---------|--------|------|
| 读取敏感文件（`.env`、SSH 私钥、凭据文件等） | 审计记录 | 规则可增删 | 不阻断（读取本身常见于正常工作流），供取证与评分联动 |
| 读取 `.git` 目录下任何文件 | **阻断** + 记录 | 可改为审计 | 窃取 `.git` = 窃取全仓库历史与凭据配置 |
| 打包行为 | **阻断** + 记录 | 可改为审计 | 见下方双信号判定 |

**打包行为判定（双信号，任一命中即判定）**：
1. harness 进程树内出现归档命令（`tar` / `zip` / `7z` / `gzip` 等）；
2. harness 进程树内进程创建压缩格式产物文件（`.zip` / `.tar.gz` / `.7z` 等）——覆盖进程内库打包（如 Node 的 archiver 库），无独立子进程的场景。

**核心矛盾的设计依据**：harness 的正常工作就是大量读源码（代码索引、全仓分析），"读文件"本身不构成阻断依据，只作为行为画像与网络层联动评分的输入。

### 3.2 网络层（兜底防线）

- **判定信号**：上行流量字节量超过阈值（默认 **100MB**，用户可预设、可自设）+ 目标端点不在白名单 → 阻断。
- **依据**：正常 LLM API 流量不可能产生百 MB 级别上行；百 MB 级上行意味着批量数据外传。
- **可观测数据**：socket 层五元组（源/目的 IP、端口）、连接字节量、时间戳、发起进程关联。
- **域名→IP 关联**：监控 DNS 查询建立映射，白名单按域名维护（用户友好）；DoH/DoT 下退化为按 IP 判定。
- **记录粒度**：仅元数据（五元组 + 字节量 + 时间戳 + 进程关联），不存 payload。
- **明确不做**：TLS MITM 明文检测（需装根证书、侵入性极强、Node 系 harness 可能证书校验失败直接报错）。

### 3.3 豁免机制（身份矩阵）

| 进程身份 | 文件操作豁免情况 |
|----------|------------------|
| git 等工具链可执行文件自身 | 读 `.git` 等本职操作**豁免**（否则 `git status`/`git commit` 全被打断） |
| harness 主进程 / 其他子进程 | 命中阻断规则即阻断 |
| harness 进程树内的 git 子进程 | **导出型命令封堵**：`git archive` / `git bundle` / `git format-patch` 一律阻断（按命令行参数判定），堵住"借 git 子进程导出仓库"的绕过路径 |

### 3.4 进程监控

- **进程身份表**：通过 exec 事件流维护——可执行路径命中 harness 名单即标记为监控根，其子进程继承标记。
- 该机制天然覆盖任意拉起方式：直接启动、shell 链、计划任务 / cron 触发、第三方进程拉起（对应 intro 第 3、4、5 行的统一落地）。
- **harness 识别**：内置特征库（已知安装路径模式、可执行文件名 / 哈希）+ 用户通过 Web UI 手动添加安装路径。
- 事件流机制下短命子进程（如数百毫秒退出的 `git status`）不丢失；对短命进程本身的处置意义有限，重点是拦住其副作用（文件写入、网络外发，由上述规则覆盖）。

### 3.5 持久化行为告警

harness 注册计划任务 / cron / 自启动项 → **高可疑告警**（审计不阻断）。这是恶意持久化信号，正常 harness 工作流不应出现此行为。

---

## 4. 响应动作

1. **自动阻断**：规则命中即执行，无需人工值守（依据已确认的响应模式）。
2. **OS 级通知**：阻断发生时弹操作系统原生通知（Windows Toast / macOS 通知中心 / Linux `notify-send`），**不用**浏览器弹窗（浏览器可能未开、通知不可达）。
3. **白名单**：用户可对规则命中的端点 / 路径 / 进程添加豁免，避免重复误伤。

---

## 5. 平台能力阶梯

统一架构模型：**事件流（检测）+ 处置动作（响应）**，macOS 仅处置能力弱一档，代码架构三平台一致。

| 平台 | 检测接口 | 阻断能力 | 阻断实时性 |
|------|----------|----------|-----------|
| **Linux** | fanotify（文件）、eBPF（进程/网络事件） | fanotify `FAN_OPEN_PERM` 同步允许/拒绝文件打开；nftables 按目标 IP 阻断网络 | **实时同步** |
| **Windows** | ETW（进程/文件/网络事件，管理员权限，无需驱动） | 用户态 WFP / 防火墙规则（`FwpmFilterAdd`）+ 杀进程 + 重置 TCP 连接；**无需签名驱动** | 近实时（毫秒级延迟） |
| **macOS** | auditpipe / BSM（exec/文件事件，含短命进程）、FSEvents、libpcap（网络） | `tcpdrop` 掐连接 + pf 规则封目标 IP + 杀进程 | **事后秒级处置**（无公开 API 的实时文件阻断路径） |

**macOS 约束说明**：进程/文件实时同步阻断的唯一正路是 EndpointSecurity.framework，其 entitlement Apple 仅批给安全厂商，个人 / 开源项目不可得，已明确放弃申请。

---

## 6. 系统架构

### 6.1 进程模型

**单特权系统服务**（一个 Rust 二进制）：
- 以系统服务运行：Windows Service / systemd / LaunchDaemon，开机自启即保护。
- 安装 / 升级时一次 UAC / sudo，运行期无感。
- 同时承载：监控引擎 + 内嵌 Web UI + 通知发送。

### 6.2 Web UI 安全

- 仅监听 `127.0.0.1`（不暴露局域网）。
- 服务启动时生成随机 token，访问须携带；首次访问由安装器打印 URL / 自动打开。
- 校验 `Host` header，防 DNS rebinding 攻击。
- 该方案与"被动外传 + 基础加固"威胁模型匹配；双进程（特权服务 + 用户态 UI 网关）方案在已定威胁模型下性价比不高，不采用。

### 6.3 自保护（基础加固，对应威胁模型）

- 配置 / 规则文件仅管理员可写（ACL）。
- 审计日志只能追加，不可篡改。
- 服务异常退出自动重启（服务管理器恢复策略）。
- 监控自身被杀 / 被篡改时发 OS 级告警。
- **不承诺**"不可绕过"，仅将绕过成本拉到"必须显式主动作恶"级别。

### 6.4 资源约束

- 整体内存 ≤ **100MB**（Rust 无 GC，守护进程 RSS 预期 5–15MB 起步，余量充足）。
- CPU 低水平：事件流异步处理，规则匹配走轻量路径。

---

## 7. 数据与配置

- **事件日志**：SQLite 单文件（rusqlite），追加写 + 文件 ACL 仅管理员可写；记录文件访问 / 进程 / 网络元数据 / 阻断决策。
- **滚动清理**：默认保留 30 天或 500MB（先到为准），均可配置。
- **配置**：TOML 文件与 Web UI 双通道修改，均落盘为同一份配置，仅管理员可写。
- **流量记录**：仅元数据，不含 payload。

---

## 8. 与 intro.md 的差异修正记录

| intro 原文 | 修正 | 原因 |
|------------|------|------|
| 第 2 行"**可能**需要监控网络流量" | 改为**必备** | "阻止上传"目标依赖网络感知与阻断，非可选项 |
| 第 8 行"运行访问内核态，需要运行时提权" | 改为**用户态特权接口**（ETW / fanotify / eBPF / auditpipe），无内核驱动 | 三平台均已存在用户态特权 API 路径；内核驱动需签名，成本失控；"提权" = 管理员 / root 系统服务 |
| 第 9 行"需要有简单的 GUI" | 统一为**纯 Web UI**（内嵌于特权服务） | 排除桌面 GUI 框架的内存开销；浏览器访问已满足需求 |
| 第 6 行"必要时……使用独立一套代码" | 统一 **Rust 代码库 + 平台适配层** | 规则引擎 / UI / 存储三平台共享，仅平台事件源与处置动作走适配层 |

---

## 9. 明确的非目标（Non-Goals）

1. TLS MITM 明文检测。
2. 内核驱动 / WHQL 签名 / EndpointSecurity entitlement 申请。
3. 防"定向恶意逃逸"的强对抗（内核级自保护）。
4. 存储 / 检测网络 payload 内容。
5. 企业集中管控（当前定位为个人单机工具，无中心上报）。

---

## 10. 开放项（后续细化）

- 内置规则默认值集合：敏感文件清单、归档命令 / 产物格式清单、git 导出命令封堵清单的完整枚举。
- 主流 harness 特征库：Claude Code / Codex / Cursor 等的安装路径模式与可执行文件清单。
- 网络字节量的平台采集细节：连接粒度字节计数的稳定性（UDP / QUIC 流量归因）。
- 服务自升级机制（若做）：升级包签名校验。
- Web UI 功能清单：事件流浏览、搜索、白名单管理、配置修改、阈值调整。
